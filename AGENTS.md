# niumateam 协作规范

niumateam 是一套自动化软件开发流水线：任务进入队列后，由包工头派发给隔离的开发 Agent，再由独立的审查 Agent 验收，最后交付人工合并。

## 维护方式

- 本文件是 AI 协作流程、review 政策和项目边界的统一入口。
- Codex 直接读取本文件；Claude Code 通过根目录 `CLAUDE.md` 导入本文件。
- 当前仓库主要是产品与架构说明；实现、脚本和配置加入后，必须把新的可验证事实同步到 README、docs 和本文件。

## 文档与沟通语言

- 流程与约束文档、GitHub issue/PR 正文和 review 全程使用中文；Issue 标题使用中文。
- PR 标题使用英文 conventional commit 格式（`feat:` / `fix:` / `docs:` / `test:` / `refactor:` / `chore:` 等）。
- 产品 README、设计文档和用户界面文案沿用现有中文；代码标识符和 commit message 保持英文。

## 项目边界与核心不变量

- 系统负责任务队列、角色分工、Agent 会话恢复、工作区隔离、验证结果回传、成本控制和人工合并关卡。
- 开发 Agent 与审查 Agent 必须保持独立上下文；开发者不能替自己完成最终验收，审查结果必须可追溯到任务验收标准和实际证据。
- 任务状态必须可恢复、可追踪且不因 Agent 崩溃或超时而丢失；返工必须保留原始审查反馈和关联证据。
- Agent、代码托管平台、模型和部署环境通过抽象层接入，不把任何一家服务商、仓库平台或模型写成不可替换的产品边界。
- 默认面向私有仓库和可信环境；权限、凭据、预算、并发上限、重试策略和人工关卡必须 fail closed，不得为方便联调而扩大到公网。
- 破坏任务状态、队列语义、工作区隔离、权限或人工审批边界的变更，必须有迁移/兼容方案和端到端验证。

## 范围纪律

一个 PR 只关闭一个可实施的 task issue。开始前完整阅读 issue 指向的实现、测试、README、设计文档、状态机和部署路径；发现的既有缺陷是邻近债务，必须单独开 issue 并在目标 issue 的“已知邻近问题”中链接。

一个 issue 只有在以下条件都满足时才可标记 `status: ready`：

1. 写清 `## 范围`（含明确排除项）和可逐条验证的 `## 验收标准`。
2. 安全、权限、成本或健壮性任务写清保证、不保证和调用方责任；不适用时明确写“无”。
3. 将要改动的实现、测试、状态机、契约和运维路径已经完整读过。
4. 邻近债务已经各自开成 issue 并完成链接；确认没有时明确写“无”。
5. 前置 issue 已关闭，GitHub 原生依赖关系与标签一致。

实施中以 issue 范围为约束：不改变明确排除的行为，不顺手重构、改名、升级 Agent/平台适配或修复相邻缺陷；先找不变量，再在路径汇合处修复并覆盖相关输入集合。成功路径以及适用的失败、取消、超时、重试、权限、资源、成本和并发行为必须与实现一起测试。若验收标准确实要求越过原范围，先更新或拆分 issue。

Review 意见先分类：本 PR 新增/实质修改的缺陷在本 PR 修复；既有或仅与 diff 相邻的问题单独开 issue 并链接，不能顺手修复。只有既有缺陷导致本 PR 某条验收标准无法验证时，才可作为越界例外，并明确指出该条标准。PR 进入第三轮 review 时暂停写代码，逐 commit 审计范围；不能追溯到验收标准的改动移出并改为 follow-up issue。

## 安全与变更纪律

- 不得提交 Agent/API key、OAuth token、仓库凭据、模型凭据、工作区私有内容、任务输入或用户数据。
- 日志、工件、截图和审查报告必须脱敏；不能把凭据或完整私有任务内容回传到不受信任的 Agent/平台。
- 外部命令、代码执行、文件系统和 Git 操作必须有明确边界；删除、覆盖、强推或发布操作需要人工关卡和精确目标。
- 只读分析不得修改文件；保留用户已有且与任务无关的改动，不回退、覆盖、提交或推送它们。
- 提交前检查文档链接、模板格式、secret、权限边界和仓库状态。

## 验证

当前仓库没有可执行代码或自动化测试。每个文档/设计 PR 至少运行并记录：

```bash
git diff --check
```

加入实现后，必须补充组件单测、队列/状态机恢复测试、Agent/平台适配测试和端到端验收；涉及真实外部平台时记录凭据注入、权限范围、清理和失败回滚方式。

## 合并 PR 后

1. 确认远端 PR 已合并，目标分支包含结果；检查并按完成程度处理唯一关联 issue，无关联时明确说明。
2. 确认远端工作分支已删除；保留时说明原因。
3. 用 `git worktree list` 检查并安全清理不再需要的 worktree，然后运行 `git worktree prune`。
4. 用 `git switch main && git merge --ff-only origin/main` 更新目标分支；若被其他 worktree 占用，先处理占用者并说明。
5. 通过 PR 状态或实质 diff 确认改动已进入目标分支后，再清理本地工作分支和远端跟踪引用；不要把 `git branch -d` 失败当作未合并证据。
6. 汇报 PR、issue、远端/本地分支、worktree 和验证结果；未完成的清理必须说明原因与后续动作。
